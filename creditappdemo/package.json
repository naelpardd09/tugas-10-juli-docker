{
  "name": "creditappdemo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "pretest:e2e": "playwright install chromium",
    "test:e2e": "playwright test",
    "pretest:e2e:intro": "playwright install chromium",
    "test:e2e:intro": "playwright test tests/workshop/intro",
    "setup:e2e": "playwright install chromium",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:trace": "PW_TRACE=on playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "vite": "^6.3.5"
  }
}
